<!DOCTYPE html> <html lang="nl"> <head>    <meta charset="UTF-8">    <meta name="viewport" content="width=device-width, initial-scale=1.0">    <title>Hoveniersdesk - Klantbeheer voor Hoveniers</title>    <link rel="preconnect" href="https://fonts.googleapis.com">    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>    <link href="https://fonts.googleapis.com/css2?family=Merriweather:wght@700;900&family=Lato:wght@400;700&display=swap" rel="stylesheet">    <script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>    <style>        :root {            --groen-donker: #2d5016;            --groen-medium: #4a7c2c;            --groen-licht: #7ba65d;            --groen-accent: #9bc47d;            --aarde: #5d4e37;            --zand: #f4f1e8;            --wit: #fefefe;            --schaduw: rgba(45, 80, 22, 0.15);        }
       * {            margin: 0;            padding: 0;            box-sizing: border-box;        }
       body {            font-family: 'Lato', sans-serif;            background: linear-gradient(135deg, var(--zand) 0%, #e8e4d9 100%);            color: var(--groen-donker);            min-height: 100vh;            position: relative;            overflow-x: hidden;        }
       body::before {            content: '';            position: fixed;            top: 0;            left: 0;            right: 0;            bottom: 0;            background-image:                 radial-gradient(circle at 20% 80%, rgba(155, 196, 125, 0.1) 0%, transparent 50%),                radial-gradient(circle at 80% 20%, rgba(74, 124, 44, 0.08) 0%, transparent 50%);            pointer-events: none;            z-index: 0;        }
       .container {            max-width: 1400px;            margin: 0 auto;            padding: 2rem;            position: relative;            z-index: 1;        }
       /* Login scherm styling */        .login-container {            max-width: 450px;            margin: 8rem auto;            background: var(--wit);            padding: 3rem;            border-radius: 16px;            box-shadow: 0 8px 32px var(--schaduw);        }
       .login-container h1 {            font-family: 'Merriweather', serif;            color: var(--groen-donker);            margin-bottom: 0.5rem;            text-align: center;            font-size: 2.2rem;        }
       .login-container .subtitle {            text-align: center;            color: var(--groen-medium);            margin-bottom: 2rem;            font-size: 1rem;        }
       .login-form {            display: flex;            flex-direction: column;            gap: 1.25rem;        }
       .login-form input {            padding: 1rem;            border: 2px solid #d4cfbf;            border-radius: 8px;            font-size: 1rem;            font-family: 'Lato', sans-serif;            transition: all 0.2s;            background: var(--zand);        }
       .login-form input:focus {            outline: none;            border-color: var(--groen-medium);            background: var(--wit);            box-shadow: 0 0 0 3px rgba(74, 124, 44, 0.1);        }
       .login-form button {            padding: 1rem;            background: var(--groen-medium);            color: var(--wit);            border: none;            border-radius: 8px;            font-size: 1.1rem;            font-weight: 700;            cursor: pointer;            transition: all 0.2s;            font-family: 'Lato', sans-serif;        }
       .login-form button:hover {            background: var(--groen-donker);            transform: translateY(-2px);            box-shadow: 0 4px 12px var(--schaduw);        }
       .login-form button:disabled {            background: #ccc;            cursor: not-allowed;            transform: none;        }
       .toggle-auth {            text-align: center;            margin-top: 1.5rem;            color: var(--groen-medium);        }
       .toggle-auth a {            color: var(--groen-donker);            font-weight: 700;            cursor: pointer;            text-decoration: none;        }
       .toggle-auth a:hover {            text-decoration: underline;        }
       .error-message {            background: #fee;            color: #c33;            padding: 1rem;            border-radius: 8px;            margin-bottom: 1rem;            border-left: 4px solid #c33;        }
       .success-message {            background: #efe;            color: #3a3;            padding: 1rem;            border-radius: 8px;            margin-bottom: 1rem;            border-left: 4px solid #3a3;        }
       .dashboard {            display: grid;            grid-template-columns: repeat(4, 1fr);            gap: 1.5rem;            margin-bottom: 2rem;        }
       .stat-kaart {            background: var(--wit);            padding: 1.5rem;            border-radius: 12px;            box-shadow: 0 4px 16px var(--schaduw);            border-top: 4px solid var(--groen-medium);            transition: transform 0.2s;        }
       .stat-kaart:hover {            transform: translateY(-4px);            box-shadow: 0 8px 24px var(--schaduw);        }
       .stat-label {            font-size: 0.9rem;            color: var(--groen-medium);            margin-bottom: 0.5rem;            font-weight: 700;        }
       .stat-waarde {            font-size: 2rem;            font-weight: 900;            color: var(--groen-donker);            font-family: 'Merriweather', serif;        }
       header {            background: linear-gradient(135deg, var(--groen-donker) 0%, var(--groen-medium) 100%);            padding: 2rem 3rem;            border-radius: 16px;            margin-bottom: 2.5rem;            box-shadow: 0 8px 24px var(--schaduw);            position: relative;            overflow: hidden;            display: flex;            justify-content: space-between;            align-items: center;        }
       header::after {            content: '🌿';            position: absolute;            right: 2rem;            top: 50%;            transform: translateY(-50%);            font-size: 4rem;            opacity: 0.15;        }
       .header-left h1 {            font-family: 'Merriweather', serif;            font-size: 2.5rem;            color: var(--wit);            margin-bottom: 0.5rem;            font-weight: 900;            letter-spacing: -0.02em;        }
       .subtitle {            color: var(--groen-accent);            font-size: 1.1rem;            font-weight: 400;        }
       .logout-btn {            background: rgba(255, 255, 255, 0.2);            color: var(--wit);            border: 2px solid var(--wit);            padding: 0.7rem 1.5rem;            border-radius: 8px;            cursor: pointer;            font-weight: 700;            transition: all 0.2s;            z-index: 10;        }
       .logout-btn:hover {            background: var(--wit);            color: var(--groen-donker);        }
       .main-grid {            display: grid;            grid-template-columns: 350px 1fr;            gap: 2rem;            margin-bottom: 2rem;        }
       .klanten-lijst {            background: var(--wit);            border-radius: 12px;            padding: 1.5rem;            box-shadow: 0 4px 16px var(--schaduw);            max-height: calc(100vh - 250px);            overflow-y: auto;        }
       .klanten-lijst h2 {            font-family: 'Merriweather', serif;            font-size: 1.3rem;            color: var(--groen-donker);            margin-bottom: 1.25rem;            display: flex;            align-items: center;            gap: 0.5rem;        }
       .klanten-lijst h2::before {            content: '🏡';            font-size: 1.5rem;        }
       .zoek-input {            width: 100%;            padding: 0.75rem 1rem;            border: 2px solid var(--groen-licht);            border-radius: 8px;            font-size: 0.95rem;            margin-bottom: 1rem;            font-family: 'Lato', sans-serif;            transition: all 0.2s;        }
       .zoek-input:focus {            outline: none;            border-color: var(--groen-medium);            box-shadow: 0 0 0 3px rgba(74, 124, 44, 0.1);        }
       .klant-item {            padding: 1rem;            background: var(--zand);            border-radius: 8px;            margin-bottom: 0.75rem;            cursor: pointer;            transition: all 0.2s;            border-left: 4px solid var(--groen-accent);        }
       .klant-item:hover {            background: var(--wit);            box-shadow: 0 2px 8px var(--schaduw);            transform: translateX(4px);        }
       .klant-item.actief {            background: var(--groen-licht);            color: var(--wit);            border-left-color: var(--groen-donker);        }
       .klant-naam {            font-weight: 700;            margin-bottom: 0.25rem;            font-size: 1rem;        }
       .klant-info {            font-size: 0.85rem;            opacity: 0.8;        }
       .klant-item.actief .klant-info {            opacity: 0.95;        }
       .nieuwe-klant-btn {            width: 100%;            padding: 1rem;            background: var(--groen-medium);            color: var(--wit);            border: none;            border-radius: 8px;            font-size: 1rem;            font-weight: 700;            cursor: pointer;            transition: all 0.2s;            font-family: 'Lato', sans-serif;            margin-top: 1rem;        }
       .nieuwe-klant-btn:hover {            background: var(--groen-donker);            transform: translateY(-2px);            box-shadow: 0 4px 12px var(--schaduw);        }
       .klant-detail {            background: var(--wit);            border-radius: 12px;            padding: 2rem;            box-shadow: 0 4px 16px var(--schaduw);        }
       .klant-detail h2 {            font-family: 'Merriweather', serif;            font-size: 1.8rem;            color: var(--groen-donker);            margin-bottom: 1.5rem;        }
       .form-grid {            display: grid;            grid-template-columns: 1fr 1fr;            gap: 1.25rem;            margin-bottom: 2rem;        }
       .form-grid.full {            grid-template-columns: 1fr;        }
       .form-groep {            display: flex;            flex-direction: column;        }
       label {            font-weight: 700;            color: var(--groen-donker);            margin-bottom: 0.5rem;            font-size: 0.9rem;            display: flex;            align-items: center;            gap: 0.4rem;        }
       .label-icon {            font-size: 1.1rem;        }
       input, textarea, select {            padding: 0.75rem;            border: 2px solid #d4cfbf;            border-radius: 6px;            font-size: 0.95rem;            font-family: 'Lato', sans-serif;            transition: all 0.2s;            background: var(--zand);        }
       input:focus, textarea:focus, select:focus {            outline: none;            border-color: var(--groen-medium);            background: var(--wit);            box-shadow: 0 0 0 3px rgba(74, 124, 44, 0.1);        }
       textarea {            resize: vertical;            min-height: 100px;        }
       .sectie {            background: linear-gradient(135deg, var(--zand) 0%, var(--wit) 100%);            padding: 1.5rem;            border-radius: 10px;            margin-bottom: 1.5rem;            border: 2px solid var(--groen-accent);        }
       .sectie h3 {            font-family: 'Merriweather', serif;            color: var(--groen-medium);            margin-bottom: 1rem;            font-size: 1.2rem;            display: flex;            align-items: center;            gap: 0.5rem;        }
       .actie-knoppen {            display: flex;            gap: 1rem;            margin-top: 2rem;        }
       .btn {            padding: 0.9rem 1.8rem;            border: none;            border-radius: 8px;            font-size: 1rem;            font-weight: 700;            cursor: pointer;            transition: all 0.2s;            font-family: 'Lato', sans-serif;        }
       .btn-primair {            background: var(--groen-medium);            color: var(--wit);            flex: 1;        }
       .btn-primair:hover {            background: var(--groen-donker);            transform: translateY(-2px);            box-shadow: 0 4px 12px var(--schaduw);        }
       .btn-secundair {            background: var(--aarde);            color: var(--wit);        }
       .btn-secundair:hover {            background: #4a3f2e;            transform: translateY(-2px);            box-shadow: 0 4px 12px rgba(93, 78, 55, 0.3);        }
       .placeholder-text {            text-align: center;            color: var(--groen-medium);            padding: 4rem 2rem;            font-size: 1.1rem;        }
       .placeholder-text::before {            content: '🌱';            display: block;            font-size: 4rem;            margin-bottom: 1rem;            opacity: 0.5;        }
       .status-badge {            display: inline-block;            padding: 0.3rem 0.8rem;            border-radius: 20px;            font-size: 0.8rem;            font-weight: 700;            margin-left: 0.5rem;        }
       .status-actief {            background: var(--groen-accent);            color: var(--groen-donker);        }
       .status-inactief {            background: #ccc;            color: #666;        }
       .hidden {            display: none;        }
       .loading {            text-align: center;            padding: 3rem;            color: var(--groen-medium);            font-size: 1.2rem;        }
       @media (max-width: 1024px) {            .main-grid {                grid-template-columns: 1fr;            }
           .dashboard {                grid-template-columns: repeat(2, 1fr);            }
           .klanten-lijst {                max-height: 400px;            }        }
       @media (max-width: 640px) {            .dashboard {                grid-template-columns: 1fr;            }        }    </style> </head> <body>    <!-- Login/Register scherm -->    <div id="authContainer" class="login-container">        <h1>🌿 Hoveniersdesk</h1>        <p class="subtitle">Klantbeheer voor hoveniersbedrijven</p>                <div id="authMessage"></div>                <form id="authForm" class="login-form">            <input type="email" id="authEmail" placeholder="E-mailadres" required>            <input type="password" id="authPassword" placeholder="Wachtwoord" required>            <button type="submit" id="authSubmitBtn">Inloggen</button>        </form>                <div class="toggle-auth">            <span id="authToggleText">Nog geen account?</span>            <a id="authToggleLink" onclick="toggleAuthMode()">Registreren</a>        </div>    </div>
   <!-- Hoofd applicatie (verborgen tot ingelogd) -->    <div id="appContainer" class="container hidden">        <header>            <div class="header-left">                <h1>Hoveniersdesk</h1>                <p class="subtitle">Klantbeheer voor hoveniersbedrijven</p>            </div>            <button class="logout-btn" onclick="uitloggen()">Uitloggen</button>        </header>
       <div class="dashboard">            <div class="stat-kaart">                <div class="stat-label">Actieve Klanten</div>                <div class="stat-waarde" id="actieveKlanten">0</div>            </div>            <div class="stat-kaart">                <div class="stat-label">Week Omzet</div>                <div class="stat-waarde" id="weekOmzet">€0</div>            </div>            <div class="stat-kaart">                <div class="stat-label">Maand Verwacht</div>                <div class="stat-waarde" id="maandVerwacht">€0</div>            </div>            <div class="stat-kaart">                <div class="stat-label">Vandaag Afspraken</div>                <div class="stat-waarde" id="vandaagAfspraken">0</div>            </div>        </div>
       <div class="main-grid">            <aside class="klanten-lijst">                <h2>Klanten</h2>                <input type="text" class="zoek-input" id="zoekInput" placeholder="Zoek klant...">                <div id="klantenLijst"></div>                <button class="nieuwe-klant-btn" onclick="nieuweKlant()">+ Nieuwe Klant</button>            </aside>
           <main class="klant-detail" id="klantDetail">                <div class="placeholder-text">                    Selecteer een klant of maak een nieuwe aan                </div>            </main>        </div>    </div>
   <script>        // ===== SUPABASE CONFIGURATIE =====        // BELANGRIJK: Vervang deze waarden met je eigen Supabase project credentials        const SUPABASE_URL = 'Hoveniersdesk'; //         const SUPABASE_ANON_KEY = 'sb_publishable_CGDBuAT9zQF_NKmbl0y6xQ_jaNPuWC8';                const supabase = window.supabase.createClient(SUPABASE_URL, SUPABASE_ANON_KEY);
       // ===== GLOBALE VARIABELEN =====        let klanten = [];        let huidigeKlantId = null;        let huidigeGebruiker = null;        let isLoginMode = true;
       // ===== AUTHENTICATIE FUNCTIES =====                // Check of gebruiker is ingelogd bij pagina laden        async function checkAuth() {            const { data: { user } } = await supabase.auth.getUser();                        if (user) {                huidigeGebruiker = user;                toonApp();                await laadKlanten();            } else {                toonAuth();            }        }
       // Login/Register form handler        document.getElementById('authForm').addEventListener('submit', async (e) => {            e.preventDefault();                        const email = document.getElementById('authEmail').value;            const password = document.getElementById('authPassword').value;            const submitBtn = document.getElementById('authSubmitBtn');                        submitBtn.disabled = true;            submitBtn.textContent = isLoginMode ? 'Inloggen...' : 'Registreren...';                        try {                let result;                                if (isLoginMode) {                    // Inloggen                    result = await supabase.auth.signInWithPassword({                        email: email,                        password: password                    });                } else {                    // Registreren                    result = await supabase.auth.signUp({                        email: email,                        password: password                    });                }                                if (result.error) throw result.error;                                if (!isLoginMode) {                    toonBericht('Account aangemaakt! Check je email voor verificatie.', 'success');                    setTimeout(() => {                        isLoginMode = true;                        toggleAuthMode();                    }, 2000);                } else {                    huidigeGebruiker = result.data.user;                    toonApp();                    await laadKlanten();                }                            } catch (error) {                console.error('Auth error:', error);                toonBericht(error.message || 'Er ging iets mis. Probeer opnieuw.', 'error');            } finally {                submitBtn.disabled = false;                submitBtn.textContent = isLoginMode ? 'Inloggen' : 'Registreren';            }        });
       function toggleAuthMode() {            isLoginMode = !isLoginMode;                        const submitBtn = document.getElementById('authSubmitBtn');            const toggleText = document.getElementById('authToggleText');            const toggleLink = document.getElementById('authToggleLink');                        if (isLoginMode) {                submitBtn.textContent = 'Inloggen';                toggleText.textContent = 'Nog geen account?';                toggleLink.textContent = 'Registreren';            } else {                submitBtn.textContent = 'Registreren';                toggleText.textContent = 'Al een account?';                toggleLink.textContent = 'Inloggen';            }                        document.getElementById('authMessage').innerHTML = '';        }
       async function uitloggen() {            await supabase.auth.signOut();            huidigeGebruiker = null;            klanten = [];            toonAuth();        }
       function toonAuth() {            document.getElementById('authContainer').classList.remove('hidden');            document.getElementById('appContainer').classList.add('hidden');        }
       function toonApp() {            document.getElementById('authContainer').classList.add('hidden');            document.getElementById('appContainer').classList.remove('hidden');        }
       function toonBericht(tekst, type) {            const messageDiv = document.getElementById('authMessage');            messageDiv.innerHTML = `<div class="${type}-message">${tekst}</div>`;        }
       // ===== DATABASE FUNCTIES =====
       async function laadKlanten() {            try {                const { data, error } = await supabase                    .from('klanten')                    .select('*')                    .eq('user_id', huidigeGebruiker.id)                    .order('naam');                                if (error) throw error;                                klanten = data || [];                renderKlantenLijst();                updateDashboard();                                if (klanten.length > 0 && !huidigeKlantId) {                    toonKlant(klanten[0].id);                }            } catch (error) {                console.error('Error loading klanten:', error);                alert('Fout bij laden van klanten: ' + error.message);            }        }
       async function opslaanKlant() {            if (!huidigeKlantId) return;                        const klant = klanten.find(k => k.id === huidigeKlantId);            if (!klant) return;                        try {                const { error } = await supabase                    .from('klanten')                    .update({                        naam: klant.naam,                        adres: klant.adres,                        telefoon: klant.telefoon,                        email: klant.email,                        tuintype: klant.tuintype,                        tuingrootte: klant.tuingrootte,                        grondsoort: klant.grondsoort,                        opmerkingen: klant.opmerkingen,                        onderhoud: klant.onderhoud,                        prijs_per_bezoek: klant.prijsPerBezoek,                        hekcode: klant.hekcode,                        laatste_bezoek: klant.laatsteBezoek,                        volgende_bezoek: klant.volgendeBezoek,                        status: klant.status                    })                    .eq('id', huidigeKlantId);                                if (error) throw error;                                alert('✅ Klantgegevens opgeslagen!');                await laadKlanten();            } catch (error) {                console.error('Error saving klant:', error);                alert('Fout bij opslaan: ' + error.message);            }        }
       async function nieuweKlant() {            try {                const { data, error } = await supabase                    .from('klanten')                    .insert({                        user_id: huidigeGebruiker.id,                        naam: "Nieuwe klant",                        adres: "",                        telefoon: "",                        email: "",                        tuintype: "achtertuin",                        tuingrootte: 100,                        grondsoort: "zand",                        opmerkingen: "",                        onderhoud: "maandelijks",                        prijs_per_bezoek: 75,                        hekcode: "",                        laatste_bezoek: null,                        volgende_bezoek: null,                        status: "actief"                    })                    .select()                    .single();                                if (error) throw error;                                await laadKlanten();                toonKlant(data.id);            } catch (error) {                console.error('Error creating klant:', error);                alert('Fout bij aanmaken klant: ' + error.message);            }        }
       async function verwijderKlant(id) {            if (!confirm('Weet je zeker dat je deze klant wilt verwijderen?')) return;                        try {                const { error } = await supabase                    .from('klanten')                    .delete()                    .eq('id', id);                                if (error) throw error;                                huidigeKlantId = null;                document.getElementById('klantDetail').innerHTML = `                    <div class="placeholder-text">                        Klant verwijderd. Selecteer een andere klant of maak een nieuwe aan.                    </div>                `;                                await laadKlanten();            } catch (error) {                console.error('Error deleting klant:', error);                alert('Fout bij verwijderen: ' + error.message);            }        }
       // ===== UI FUNCTIES =====
       function updateDashboard() {            const actief = klanten.filter(k => k.status === 'actief').length;            document.getElementById('actieveKlanten').textContent = actief;
           const vandaag = new Date();            const weekStart = new Date(vandaag);            weekStart.setDate(vandaag.getDate() - vandaag.getDay());            const weekEind = new Date(weekStart);            weekEind.setDate(weekStart.getDate() + 7);
           let weekOmzet = 0;            let vandaagAfspraken = 0;
           klanten.forEach(k => {                if (k.volgende_bezoek) {                    const bezoekDatum = new Date(k.volgende_bezoek);                    if (bezoekDatum >= weekStart && bezoekDatum < weekEind) {                        weekOmzet += parseFloat(k.prijs_per_bezoek || 0);                    }                    if (bezoekDatum.toDateString() === vandaag.toDateString()) {                        vandaagAfspraken++;                    }                }            });
           document.getElementById('weekOmzet').textContent = `€${weekOmzet.toFixed(0)}`;            document.getElementById('vandaagAfspraken').textContent = vandaagAfspraken;
           let maandVerwacht = 0;            klanten.forEach(k => {                if (k.status === 'actief' && k.onderhoud) {                    const prijs = parseFloat(k.prijs_per_bezoek || 0);                    switch(k.onderhoud) {                        case 'wekelijks':                            maandVerwacht += prijs * 4;                            break;                        case 'tweewekelijks':                            maandVerwacht += prijs * 2;                            break;                        case 'maandelijks':                            maandVerwacht += prijs;                            break;                    }                }            });
           document.getElementById('maandVerwacht').textContent = `€${maandVerwacht.toFixed(0)}`;        }
       function renderKlantenLijst(filter = '') {            const lijst = document.getElementById('klantenLijst');            const gefilterd = klanten.filter(k =>                 k.naam.toLowerCase().includes(filter.toLowerCase()) ||                k.adres.toLowerCase().includes(filter.toLowerCase())            );
           lijst.innerHTML = gefilterd.map(klant => `                <div class="klant-item ${klant.id === huidigeKlantId ? 'actief' : ''}" onclick="toonKlant(${klant.id})">                    <div class="klant-naam">${klant.naam}</div>                    <div class="klant-info">${klant.adres}</div>                    <div class="klant-info">📏 ${klant.tuingrootte}m² • ${klant.tuintype}</div>                </div>            `).join('');        }
       function toonKlant(id) {            huidigeKlantId = id;            const klant = klanten.find(k => k.id === id);                        const detail = document.getElementById('klantDetail');            detail.innerHTML = `                <h2>${klant.naam} <span class="status-badge status-${klant.status}">${klant.status}</span></h2>                                <div class="sectie">                    <h3>📍 Contactgegevens</h3>                    <div class="form-grid">                        <div class="form-groep">                            <label><span class="label-icon">👤</span> Naam</label>                            <input type="text" value="${klant.naam}" onchange="updateKlant(${id}, 'naam', this.value)">                        </div>                        <div class="form-groep">                            <label><span class="label-icon">📞</span> Telefoon</label>                            <input type="tel" value="${klant.telefoon || ''}" onchange="updateKlant(${id}, 'telefoon', this.value)">                        </div>                        <div class="form-groep">                            <label><span class="label-icon">🏠</span> Adres</label>                            <input type="text" value="${klant.adres || ''}" onchange="updateKlant(${id}, 'adres', this.value)">                        </div>                        <div class="form-groep">                            <label><span class="label-icon">✉️</span> Email</label>                            <input type="email" value="${klant.email || ''}" onchange="updateKlant(${id}, 'email', this.value)">                        </div>                    </div>                </div>
               <div class="sectie">                    <h3>🌳 Tuininformatie</h3>                    <div class="form-grid">                        <div class="form-groep">                            <label><span class="label-icon">🏡</span> Type tuin</label>                            <select onchange="updateKlant(${id}, 'tuintype', this.value)">                                <option value="voortuin" ${klant.tuintype === 'voortuin' ? 'selected' : ''}>Voortuin</option>                                <option value="achtertuin" ${klant.tuintype === 'achtertuin' ? 'selected' : ''}>Achtertuin</option>                                <option value="zijgevel" ${klant.tuintype === 'zijgevel' ? 'selected' : ''}>Zijgevel</option>                                <option value="daktuin" ${klant.tuintype === 'daktuin' ? 'selected' : ''}>Daktuin</option>                            </select>                        </div>                        <div class="form-groep">                            <label><span class="label-icon">📏</span> Oppervlakte (m²)</label>                            <input type="number" value="${klant.tuingrootte}" onchange="updateKlant(${id}, 'tuingrootte', this.value)">                        </div>                        <div class="form-groep">                            <label><span class="label-icon">🌍</span> Grondsoort</label>                            <select onchange="updateKlant(${id}, 'grondsoort', this.value)">                                <option value="zand" ${klant.grondsoort === 'zand' ? 'selected' : ''}>Zand</option>                                <option value="klei" ${klant.grondsoort === 'klei' ? 'selected' : ''}>Klei</option>                                <option value="leem" ${klant.grondsoort === 'leem' ? 'selected' : ''}>Leem</option>                                <option value="veen" ${klant.grondsoort === 'veen' ? 'selected' : ''}>Veen</option>                            </select>                        </div>                        <div class="form-groep">                            <label><span class="label-icon">🔧</span> Onderhoudsfrequentie</label>                            <select onchange="updateKlant(${id}, 'onderhoud', this.value)">                                <option value="wekelijks" ${klant.onderhoud === 'wekelijks' ? 'selected' : ''}>Wekelijks</option>                                <option value="tweewekelijks" ${klant.onderhoud === 'tweewekelijks' ? 'selected' : ''}>Tweewekelijks</option>                                <option value="maandelijks" ${klant.onderhoud === 'maandelijks' ? 'selected' : ''}>Maandelijks</option>                                <option value="per-seizoen" ${klant.onderhoud === 'per-seizoen' ? 'selected' : ''}>Per seizoen</option>                                <option value="incidenteel" ${klant.onderhoud === 'incidenteel' ? 'selected' : ''}>Incidenteel</option>                            </select>                        </div>                        <div class="form-groep">                            <label><span class="label-icon">💰</span> Prijs per bezoek (€)</label>                            <input type="number" value="${klant.prijs_per_bezoek}" onchange="updateKlant(${id}, 'prijsPerBezoek', this.value)">                        </div>                        <div class="form-groep">                            <label><span class="label-icon">🔐</span> Hekcode (optioneel)</label>                            <input type="text" value="${klant.hekcode || ''}" placeholder="Bijv. 1234" onchange="updateKlant(${id}, 'hekcode', this.value)">                        </div>                    </div>                </div>
               <div class="sectie">                    <h3>📝 Notities & bijzonderheden</h3>                    <div class="form-grid full">                        <div class="form-groep">                            <label><span class="label-icon">💬</span> Opmerkingen</label>                            <textarea onchange="updateKlant(${id}, 'opmerkingen', this.value)">${klant.opmerkingen || ''}</textarea>                        </div>                    </div>                    <div class="form-grid">                        <div class="form-groep">                            <label><span class="label-icon">📅</span> Laatste bezoek</label>                            <input type="date" value="${klant.laatste_bezoek || ''}" onchange="updateKlant(${id}, 'laatsteBezoek', this.value)">                        </div>                        <div class="form-groep">                            <label><span class="label-icon">🗓️</span> Volgend bezoek</label>                            <input type="date" value="${klant.volgende_bezoek || ''}" onchange="updateKlant(${id}, 'volgendeBezoek', this.value)">                        </div>                    </div>                </div>
               <div class="actie-knoppen">                    <button class="btn btn-primair" onclick="opslaanKlant()">💾 Opslaan</button>                    <button class="btn btn-secundair" onclick="verwijderKlant(${id})">🗑️ Verwijderen</button>                </div>            `;
           renderKlantenLijst(document.getElementById('zoekInput').value);        }
       function updateKlant(id, veld, waarde) {            const klant = klanten.find(k => k.id === id);            if (klant) {                // Map frontend veldnamen naar database kolomnamen                const veldMapping = {                    'prijsPerBezoek': 'prijs_per_bezoek',                    'laatsteBezoek': 'laatste_bezoek',                    'volgendeBezoek': 'volgende_bezoek'                };                                const dbVeld = veldMapping[veld] || veld;                klant[dbVeld] = waarde;                                renderKlantenLijst(document.getElementById('zoekInput').value);            }        }
       document.getElementById('zoekInput').addEventListener('input', (e) => {            renderKlantenLijst(e.target.value);        });
       // Start de app        checkAuth();    </script> </body> </html>
